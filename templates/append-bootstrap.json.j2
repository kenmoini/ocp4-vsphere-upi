{
  "ignition": {
    "config": {
      "merge": [
        {
          "source": "http://{{ helper_node_target }}:8080/ignitions/{{ cluster_domain }}/{{ cluster_name }}/{{ 'helper' if node_item.type == "helper" }}{{ 'worker' if node_item.type == "application-node" }}{{ 'master' if node_item.type == "control-plane" }}{{ 'bootstrap' if node_item.type == "bootstrap" }}.ign"
        }
      ]
    },
    "timeouts": {},
    "version": "3.2.0"
  },
  "networkd": {},
  "passwd": {},
  "storage": {},
  "systemd": {}
}